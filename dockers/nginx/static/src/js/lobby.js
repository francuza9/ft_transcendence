// Function to update room title and details
/*
function updateRoomDetails(title, maxPlayers, map, mode) {
	document.getElementById('roomTitle').innerText = title;
	document.getElementById('roomDetails').innerText = `Max Players: ${maxPlayers} | Map: ${map} | Mode: ${mode}`;
}

// Example of how to update room details with JavaScript
updateRoomDetails('Epic Battle Room', 10, 'Forest', 'Team Deathmatch');
*/

export function viewProfile(playerId) {
	console.log('Viewing profile of:', playerId);
}
